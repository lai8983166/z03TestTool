## 1. 依赖与数据访问

- [x] 1.1 安装 `exceljs` 依赖并验证 `require('exceljs')` 可用；验证方式：`node -e "require('exceljs');console.log('ok')"`（经 npmmirror 源安装成功）
- [x] 1.2 实现 `readLatestSJCJXlsx(h, t0)`：轮询 `data/数据采集AB帧_*.xlsx` 取 t0 之后最新的文件（等待写盘稳定），读出「B帧」工作表为 `{time[], angle[]}`（表头文字定位角度列，parseFloat 解析）；验证方式：手工构造一个样例 xlsx 后单测解析（列定位、NaN 跳过）（集成测试覆盖）

## 2. 参数化搜索流程实现

- [x] 2.1 实现 `runSearchFlow(h, config)`：`setInput('comboBox_SSZL', config.搜索指令选项)` → 勾选 `comboBox_HWJHKZ` → 复位遗留保存/采集状态 → 点击 `pushButton_SJCJ_F000H_Send` 开始采集 → 点击 `pushButton_SJCJ_0010H` 开始保存 → 15 秒录制（每秒日志剩余时间）→ try/finally 清理（按按钮文本判断并停止保存/采集）→ 调用分析；验证方式：stub 下动作顺序（搜索指令在采集之前）与 finally 清理断言（T2.4/T2.1 PASS）
- [x] 2.2 实现点检测 `detectPoints(angles, config)`：九/十六波位（方位角，8帧±0.3，紧邻下一帧严格 >1）、俯仰三波位（俯仰角，5帧±0.5，round=-2）、方位三波位（方位角，5帧±0.3，round=-3）、五波位（方位角，5帧±0.3，round=-1）、四波位（方位角，prev>0 且 cur<0）；返回点锚帧索引；验证方式：构造序列单测每种规则，并覆盖九/十六波位 1.01 命中、1.0 不命中及必须紧邻下一帧
- [x] 2.3 实现指标计算：锚帧时间戳差 t21−t1、平均时间 = ÷5（ms）、范围 = 区间帧角度 max−min（度）；<21 点抛失败（含实际点数）；验证方式：合成 21 点序列断言两个指标数值（T2.3 1800ms/1.900° PASS；T2.3b 20点判失败 PASS）
- [x] 2.4 六个 `flows` 键接入 `runSearchFlow` 与各自 config；验证方式：stub 逐个运行六键，日志含动作序列与结果（SEARCH_CONFIGS 参数表 + 集成测试 PASS）

## 3. 过程日志与结果输出

- [x] 3.1 过程实时日志：录制剩余时间、分析总帧数、逐点命中行、最终"平均时间=…ms，范围=…°"两行结果；验证方式：stub 断言日志包含全部要素（T3.1 PASS）

## 4. 联机验证与提交

- [ ] 4.1 真实设备联机运行六选项：人工核对 xlsx 列名/精度、每秒约 20 帧假设、21 点可找满；校准不合理的经验参数（需真实设备，由用户执行并反馈日志）
- [x] 4.2 按"小步提交"约定 git commit（依赖 + flow.js 实现）（ffc8f0d）
- [x] 4.3 根据联机反馈将六个选项录制统一改为 15 秒，并在每次录制前复位切换式采集/保存按钮，避免首个九波位流程误将已有采集状态关闭；验证方式：stub 断言 15 次 1 秒等待及“停止旧状态→重新启动→结束清理”顺序
- [x] 4.4 根据联机反馈将九波位、十六波位的点判定由“下一帧近似2”改为“紧邻下一帧严格大于1”，新增阈值和紧邻性回归测试
