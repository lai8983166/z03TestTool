## 1. 工具函数扩展（flow.js / makeHelpers）

- [x] 1.1 新增 `setCellText(tableId, row, col, value)`：写 contentEditable 单元格 textContent；验证方式：stub 校验生成脚本与缺失报错（T1.1 PASS）
- [x] 1.2 新增转台扫描循环 `scanPositions(h, onArrived)` 与 `waitTurntableArrive(h, pitch, az)`（状态查询→读速度/位置→双条件判定，60s 超时、±0.5° 容差、环绕归一）；验证方式：stub 模拟速度归零与位置到达序列（T1.2/T2.1b PASS）

## 2. 两个流程实现

- [x] 2.1 「最小搜索范围」：勾选截获 → 开始采集 → 开始保存 → 等 5s → scanPositions（无 onArrived）→ 取消截获 → 更新A帧 → 停采集/停保存；验证方式：stub 动作顺序与收尾断言（T1.2 顺序 PASS，中止清理 PASS）
- [x] 2.2 「光轴预置范围」：勾选地面预置 → 开始采集 → 开始保存 → 等 5s → scanPositions(onArrived=设预置 25/26 行 + 更新A帧) → 取消预置 → 更新A帧 → 停采集/停保存；验证方式：stub 断言每点预置值等于点位角度（T2.2 PASS）
- [x] 2.3 前置检查（tt_serial_status 含"已连接"）与中止清理（finally：tt_btn_stop + 停采集/停保存，click/getText 增加 force 选项保证中止后清理可执行）；验证方式：stub 模拟未连接失败与中止路径（T2.3a/T2.3b PASS）

## 3. 联机验证与提交

- [ ] 3.1 真实设备联机跑两流程：核对到位判定阈值（±0.5°/60s）是否合适、状态查询应答节奏、每点停留 5s 准确性（由用户执行并反馈日志）
- [x] 3.2 按"小步提交"约定 git commit（7740010）
- [x] 3.3 根据用户澄清修正轴值下发：内环=方位、外环=俯仰，方位值写内环、俯仰值写外环且不得交换；光轴预置第 25 行=俯仰、第 26 行=方位，stub 校验输入、反馈与预置写值顺序
- [x] 3.4 修复更新A帧异步重启与停止采集的竞态：停止逻辑间隔重复核验，正常结束/异常/中止均保证A帧稳定停止；回归测试覆盖先停后被异步重启的情形
- [x] 3.5 修复关闭主界面后隐藏窗口导致进程残留：关闭时中止流程、清理页面任务、销毁隐藏窗口并退出 Electron
- [x] 3.6 两流程扫描前读取转台初始位置，8组数按[内环方位偏移, 外环俯仰偏移]分别与同一个初始位置相加，不在上一个目标上累加；光轴预置A帧写当前原始序列值而非转台绝对目标；stub 验证初始(1,2)前两目标为(1,-43)、(-14,-38)，A帧分别写(0,-45)、(-15,-40)
- [x] 3.7 初始位置采用与上位机服务相同的换算规则：每轴 `>=140°` 时减 `360°` 后作为所有点的共同计算基准；单测覆盖 139.9°、140° 与 359.8° 边界
- [x] 3.8 光轴预置范围最后一点停留后，将A帧预置俯仰角、预置方位角均写为 `0.0`，取消地面预置测试状态并更新A帧发出；回归测试断言末尾两个单元格均归零
