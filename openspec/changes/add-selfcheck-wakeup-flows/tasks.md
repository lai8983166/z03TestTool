## 1. 工具函数扩展（flow.js / makeHelpers）

- [x] 1.1 `setInput` 兼容复选框：元素 `type === 'checkbox'` 时设 `checked` 并派发 `input`+`change` 事件；用 `node -e` stub 验证生成的脚本文本含 `checked` 分支（T1.1 PASS）
- [x] 1.2 新增 `readCell(tableId, row, col)`：executeJavaScript 读取表格单元格 `textContent` 并 trim 返回；stub exec 验证返回值与"元素缺失报错"路径（T1.2 PASS）

## 2. 自检流程

- [x] 2.1 在 `flows['自检']` 中实现：记录 t0 → `click('pushButton_ZJ')` → `click('pushButton_QZJJG')` → 300ms 轮询 14 项判定清单（硬编码数组 `{row, col, name, expect}`），全部合格且用时 ≤9s 判成功；验证：node stub 下脚本文本正确、计时逻辑单测通过（T2.1/T2.1b PASS）
- [x] 2.2 失败路径：任一项"异常/未到位"或 9s 超时时立即判失败，日志输出逐项状态与总用时；验证：stub 模拟异常项与超时两种场景（T2.2/T2.2b PASS，超时实测 9144ms）
- [x] 2.3 轮询过程日志：自检轮询仅在新读取到项/发现不合格项时输出进度（不逐轮刷屏）；验证：stub 下日志序列无连续重复进度行（PASS）

## 3. 唤醒流程

- [x] 3.1 唤醒前勾选预置并启动A帧/A-B帧保存，预录100ms后点击唤醒，以点击时刻为起点继续保存2秒，finally稳定停止保存与A帧；验证stub动作顺序
- [x] 3.2 读取本次xlsx「B帧」的时间、光轴俯仰/方位列；任一通道先出现绝对值>1°、再于2秒内首次进入±0.3°即成功，以该B帧时间戳为终点；纯函数单测覆盖俯仰和方位
- [x] 3.3 失败路径区分无有效B帧、无±1°外起始样本、存在起始样本但未回到±0.3°；日志输出文件、帧数、末帧角度、终点与总用时

## 4. 联机验证与提交

- [ ] 4.1上位机 + 设备联机环境手动运行两个流程，核对日志逐项状态、用时与结论；确认判定项文本与 csv/ZJJG_Recv.csv 行列一致（需真实设备，无法自动化验证时由用户执行并反馈）
- [x] 4.2 按"小步提交"约定 git commit（flow.js 功能 + 必要的流程修正）（e20134a）
- [x] 4.3 根据联机反馈修正自检读取：忽略 CSV 初始占位值 `0`，并将右半区第 15/16/17/20 项映射为 13 行表格的 row 1/2/3/6；验证方式：stub 先返回 `0`、后返回有效状态，流程不提前失败
